<?xml version="1.0" encoding="utf-8"?>
<!--
  Release traffic is HTTPS only (Play policy + our own rule). The single
  exception is the loopback address a developer's emulator uses to reach a
  local API; it resolves to nothing on a user's device and ships no risk.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
